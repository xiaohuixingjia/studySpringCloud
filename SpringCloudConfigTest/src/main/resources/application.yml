spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths : respo
      label: master
server:
  port: 8888
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false