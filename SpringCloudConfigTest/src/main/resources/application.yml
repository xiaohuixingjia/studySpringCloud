spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaohuixingjia/studySpringCloud/SpringCloudConfigTest/
          search-paths:
            - resource
      label: master
server:
  port: 8761

